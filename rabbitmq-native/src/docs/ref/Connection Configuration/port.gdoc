h1. port

h2. Purpose

Sets the port number the RabbitMQ server is listening on.

h2. Examples

{code}
rabbitmq {
    connection = {
        connection(
            host: "rabbit.example.com",
            username: "foo",
            password: "bar",
            port: 5672
        )
    }
}
{code}

h2. Description

The port is required if the RabbitMQ is listening on a non-standard port. If omitted, the port defaults to @5672@.
