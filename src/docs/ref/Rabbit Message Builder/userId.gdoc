h1. userId

h2. Purpose

Sets the userId property of the message.

h2. Examples

{code}
new RabbitMessageBuilder().send {
    queue = "example.queue"
    userId = "foobar"
    body = "test message"
}
{code}

h2. Description

Creating user ID.
