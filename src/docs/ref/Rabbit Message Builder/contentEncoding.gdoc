h1. contentEncoding

h2. Purpose

Sets the content encoding property of the message.

h2. Examples

{code}
new RabbitMessageBuilder().send {
    queue = "example.queue"
    contentEncoding = "base64"
    body = "test message"
}
{code}

h2. Description

The content encoding field is a freeform field with no constraints. This field is typically used to pass information on how the message body was encoding, such as gzip, base64, etc.
