h1. deliveryMode

h2. Purpose

Sets the delivery mode of the message.

h2. Examples

{code}
new RabbitMessageBuilder().send {
    queue = "example.queue"
    deliveryMode = 1
    body = "test message"
}
{code}

h2. Description

Non-persistent (1) or persistent (2).
